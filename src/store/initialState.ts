import { Store } from './store'

const initialState: Store = {
    openApiConfiguration: {
        openapi: 'notDefined',
        info: undefined,
        paths: undefined
    }
}

export default initialState